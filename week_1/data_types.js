// 1. Create variable called age and assign it a number.
let age = 25;
// 2. create a variabld called name and assign it is a string.
const name = "John";
// 3. Create a variable called isStudent and assign it a boolean value.
const isStudent = true;
// Create a variable called hobbies and assign it an array with a few strings.
const hobbies = ["Football", "Site seeing"];
// Create an object called info with two properties.
const info = {
  schoolName: "Business College helsinki",
  groupName: 25,
};
// use the console log() to print each variable to the console.
console.log(age);
console.log(name);
console.log(isStudent);
console.log(hobbies);
console.log(info);
// use the typeof operator to check the type of each variable and print
console.log(typeof age);
console.log(typeof name);
console.log(typeof isStudent);
console.log(typeof hobbies);
console.log(typeof info);
